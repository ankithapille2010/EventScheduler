@model List<EventScheduler.Models.Event>

<h2>Employee List</h2>
<table>
    <tr>
        <th>ID</th>
        <th>Name</th>
    </tr>
    @foreach (var emp in Model)
    {
        <tr>
            <td>@emp.Title</td>
            <td>@emp.Date</td>
        </tr>
    }
</table>